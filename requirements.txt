# Pinned to currently-tested versions on Python 3.9.6 / macOS.
# Run `pip install -r requirements.txt` for a deterministic local setup;
# regenerate with `pip-compile --generate-hashes` per CLAUDE.md global
# supply-chain rules before any production redeploy.
DracoPy==2.0.0
Pillow==11.3.0
numpy==2.0.2
open3d==0.18.0
pygltflib==1.16.5
pyproj==3.6.1
rasterio==1.4.3
requests==2.32.5
scipy==1.13.1
shapely==2.0.7
