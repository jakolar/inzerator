# Lokace UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Webové UI pro vytvoření nové v2 lokace z adresy (RUIAN search → S-JTSK střed → spustí `gen_panorama.py` + 3× `gen_detail.py`), s dashboardem existujících lokací.

**Architecture:** Nový modul `locations.py` drží job state, FIFO frontu, worker thread spouštějící Python subprocess pipeline. `server.py` přidává `do_POST` a routuje `/api/*` na funkce z modulu. `index.html` v rootu je statická SPA stránka (vanilla JS), pollluje `/api/jobs?active=1` každé 2 s pro progress.

**Tech Stack:** Python 3 stdlib (http.server, threading, subprocess, urllib, json, unicodedata, pathlib, uuid), pytest 8, vanilla JS + fetch (žádný build).

**Spec:** `docs/superpowers/specs/2026-05-15-lokace-ui-design.md`

---

## File Structure

| Soubor | Akce | Odpovědnost |
|---|---|---|
| `locations.py` | **Nový** (~300 řádků) | Slug utils, disk scan, RUIAN search, job state + queue, worker thread, subprocess command builder |
| `server.py` | **Modifikace** | Přidat `do_POST` handler, routovat `/api/locations`, `/api/ruian/search`, `/api/jobs[?active=1]`, `/api/jobs/<id>[/retry|/cancel]` na funkce z `locations`. V `__main__` spustit `locations.start_worker()` před `serve_forever()`. |
| `index.html` | **Nový** (~400 řádků) | Dashboard + create form, polling 2 s |
| `tests/test_locations_unit.py` | **Nový** (~120 řádků) | Pure-function testy (slug, parse, status) bez živého serveru |
| `tests/test_locations_api.py` | **Nový** (~100 řádků) | Integrace proti běžícímu serveru na :8080 (mock subprocess přes test slug) |
| `cache/jobs/` | Runtime | Per-job log soubory (vznikají při běhu) |

---

## Task 1: Skeleton `locations.py` + testovací harness

**Files:**
- Create: `locations.py`
- Create: `tests/test_locations_unit.py`

- [ ] **Step 1.1: Vytvořit prázdný modul s konstantami**

```python
# locations.py
"""V2 location pipeline: address search → enqueue → worker spawns
gen_panorama.py + 3× gen_detail.py → exposes job state to UI."""
from __future__ import annotations
import threading
from pathlib import Path

TILES_DIR_PREFIX = "tiles_v2_"
JOB_LOG_DIR = Path("cache/jobs")
STEP_TIMEOUT_SECS = 60 * 60   # 60 min: cold ČÚZK DMR5G cache can take 10–30 min
STEP_NAMES = ("panorama", "outer", "closeup", "inner")

JOBS: dict[str, dict] = {}
JOB_QUEUE: list[str] = []
JOB_LOCK = threading.Lock()
JOB_CV = threading.Condition(JOB_LOCK)
CURRENT_JOB: str | None = None
```

- [ ] **Step 1.2: Sanity test import**

```python
# tests/test_locations_unit.py
import locations


def test_module_imports():
    assert locations.STEP_NAMES == ("panorama", "outer", "closeup", "inner")
    assert locations.TILES_DIR_PREFIX == "tiles_v2_"
```

- [ ] **Step 1.3: Run test**

Run: `pytest tests/test_locations_unit.py -v`
Expected: PASS (1 test)

- [ ] **Step 1.4: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): module skeleton + state placeholders"
```

---

## Task 2: Slug utilities (slugify, validate, next-free, parse RUIAN adresa)

**Files:**
- Modify: `locations.py`
- Modify: `tests/test_locations_unit.py`

- [ ] **Step 2.1: Failing tests pro 4 čisté funkce**

Připojit na konec `tests/test_locations_unit.py`:

```python
import pytest


@pytest.mark.parametrize("inp,expected", [
    ("Hnojice", "hnojice"),
    ("Strážek", "strazek"),
    ("Šternberk u Olomouce", "sternberk-u-olomouce"),
    ("  Trailing space  ", "trailing-space"),
    ("Praha 4 - Modřany", "praha-4-modrany"),
    ("ÁČĎÉĚÍŇÓŘŠŤÚŮÝŽ", "acdeeinorstuuyz"),
])
def test_slugify(inp, expected):
    assert locations.slugify(inp) == expected


@pytest.mark.parametrize("s,ok", [
    ("hnojice", True),
    ("hnojice-2", True),
    ("hnojice-statek-47", True),
    ("Hnojice", False),       # uppercase
    ("hnojice_47", False),    # underscore
    ("hnojice 47", False),    # space
    ("-hnojice", False),      # leading dash
    ("hnojice-", False),      # trailing dash
    ("", False),
])
def test_is_valid_slug(s, ok):
    assert locations.is_valid_slug(s) is ok


def test_next_free_slug_no_conflict():
    assert locations.next_free_slug("hnojice", existing=set()) == "hnojice"


def test_next_free_slug_appends_number():
    existing = {"hnojice", "hnojice-2"}
    assert locations.next_free_slug("hnojice", existing=existing) == "hnojice-3"


def test_next_free_slug_skips_holes():
    """Pokud existuje 'hnojice' a 'hnojice-5', vrátí '-2' (první volný)."""
    existing = {"hnojice", "hnojice-5"}
    assert locations.next_free_slug("hnojice", existing=existing) == "hnojice-2"


@pytest.mark.parametrize("adresa,obec", [
    ("č.p. 136, 78501 Hnojice", "Hnojice"),
    ("Jemnice 8, 59253 Strážek", "Strážek"),
    ("Hlavní 47, 100 00 Praha 10", "Praha 10"),
    ("č.p. 5, 78501 Hnojice u Šternberka", "Hnojice u Šternberka"),
])
def test_parse_obec(adresa, obec):
    assert locations.parse_obec(adresa) == obec
```

- [ ] **Step 2.2: Run, verify all 4 fail**

Run: `pytest tests/test_locations_unit.py -v`
Expected: 1 passing (import test), všechny `slugify/is_valid_slug/next_free_slug/parse_obec` selžou na `AttributeError`.

- [ ] **Step 2.3: Implementovat 4 funkce v `locations.py`**

Přidat za konstanty:

```python
import re
import unicodedata

_SLUG_RE = re.compile(r"^[a-z0-9]+(-[a-z0-9]+)*$")


def slugify(text: str) -> str:
    """NFD-rozdělit diakritiku, ASCII fold, převést mezery/non-alnum na pomlčky.
    Sekvence pomlček se collapsuje, trim leading/trailing dashes."""
    nfd = unicodedata.normalize("NFD", text)
    ascii_text = nfd.encode("ascii", "ignore").decode("ascii")
    s = re.sub(r"[^a-zA-Z0-9]+", "-", ascii_text).strip("-").lower()
    return s


def is_valid_slug(s: str) -> bool:
    return bool(s) and bool(_SLUG_RE.match(s))


def next_free_slug(base: str, existing: set[str]) -> str:
    """Pokud `base` není v `existing`, vrátí `base`. Jinak hledá první volné
    `<base>-N` pro N=2,3,4,…"""
    if base not in existing:
        return base
    n = 2
    while f"{base}-{n}" in existing:
        n += 1
    return f"{base}-{n}"


def parse_obec(adresa: str) -> str:
    """Z RUIAN adresy '<něco>, <PSČ> <Obec>' vrátí název obce.
    Adresa má vždy alespoň jednu čárku a PSČ je 5 nebo 6 znaků (s mezerou)."""
    after_comma = adresa.rsplit(",", 1)[-1].strip()
    # Odstranit PSČ (5 číslic, případně s mezerou '100 00')
    return re.sub(r"^\d{3}\s?\d{2}\s+", "", after_comma).strip()
```

- [ ] **Step 2.4: Run tests, fix until green**

Run: `pytest tests/test_locations_unit.py -v`
Expected: vše PASS (1 + 6 slugify + 9 is_valid_slug + 3 next_free_slug + 4 parse_obec = 23 tests).

Pokud něco selže (např. „Praha 4 - Modřany" → očekáváme `praha-4-modrany`), zkontrolovat `slugify`. Pomlčka mezi `Praha 4` a `Modřany` po split-on-non-alnum dá `Praha-4-Modrany`, regex collapse + lowercase → `praha-4-modrany`. OK.

- [ ] **Step 2.5: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): slug utilities (slugify, validate, next-free, parse-obec)"
```

---

## Task 3: Disk scan (list_locations, location_status, expected_glb)

**Files:**
- Modify: `locations.py`
- Modify: `tests/test_locations_unit.py`

- [ ] **Step 3.1: Failing tests s tempdir**

Připojit:

```python
def test_expected_glb_paths():
    assert locations.expected_glb("hnojice", "panorama") == Path("tiles_v2_hnojice/panorama.glb")
    assert locations.expected_glb("hnojice", "outer") == Path("tiles_v2_hnojice/details/outer.glb")
    assert locations.expected_glb("hnojice", "closeup") == Path("tiles_v2_hnojice/details/closeup.glb")
    assert locations.expected_glb("hnojice", "inner") == Path("tiles_v2_hnojice/details/inner.glb")


def test_location_status_missing(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    assert locations.location_status("nonexistent") == "missing"


def test_location_status_partial(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    (tmp_path / "tiles_v2_foo").mkdir()
    (tmp_path / "tiles_v2_foo" / "panorama.glb").touch()
    (tmp_path / "tiles_v2_foo" / "details").mkdir()
    (tmp_path / "tiles_v2_foo" / "details" / "outer.glb").touch()
    # closeup + inner chybí
    assert locations.location_status("foo") == "partial"


def test_location_status_ready(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    base = tmp_path / "tiles_v2_foo"
    (base / "details").mkdir(parents=True)
    (base / "panorama.glb").touch()
    for s in ("outer", "closeup", "inner"):
        (base / "details" / f"{s}.glb").touch()
    assert locations.location_status("foo") == "ready"


def test_list_locations_scan(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    # Dvě lokace: jedna ready, jedna partial
    for slug in ("alpha", "beta"):
        (tmp_path / f"tiles_v2_{slug}" / "details").mkdir(parents=True)
        (tmp_path / f"tiles_v2_{slug}" / "panorama.glb").touch()
    (tmp_path / "tiles_v2_alpha" / "details" / "outer.glb").touch()
    (tmp_path / "tiles_v2_alpha" / "details" / "closeup.glb").touch()
    (tmp_path / "tiles_v2_alpha" / "details" / "inner.glb").touch()
    # Manifest s label pro alpha
    import json as _json
    (tmp_path / "tiles_v2_alpha" / "manifest.json").write_text(
        _json.dumps({"region": {"slug": "alpha", "label": "Alpha Village"}}))

    result = locations.list_locations()
    by_slug = {r["slug"]: r for r in result}
    assert set(by_slug) == {"alpha", "beta"}
    assert by_slug["alpha"]["status"] == "ready"
    assert by_slug["alpha"]["label"] == "Alpha Village"
    assert by_slug["beta"]["status"] == "partial"
    assert by_slug["beta"]["label"] == "beta"   # fallback = slug
    assert by_slug["alpha"]["has_panorama"] is True
    assert by_slug["beta"]["has_outer"] is False
```

- [ ] **Step 3.2: Run, verify failures**

Run: `pytest tests/test_locations_unit.py::test_expected_glb_paths tests/test_locations_unit.py::test_location_status_missing tests/test_locations_unit.py::test_list_locations_scan -v`
Expected: 3 FAIL (AttributeError: expected_glb / location_status / list_locations).

- [ ] **Step 3.3: Implementace**

Připojit do `locations.py`:

```python
import json


def expected_glb(slug: str, step: str) -> Path:
    if step == "panorama":
        return Path(f"{TILES_DIR_PREFIX}{slug}") / "panorama.glb"
    return Path(f"{TILES_DIR_PREFIX}{slug}") / "details" / f"{step}.glb"


def location_status(slug: str) -> str:
    """'missing' = adresář nebo panorama.glb chybí.
    'partial' = panorama.glb existuje ale chybí alespoň jeden detail.
    'ready'   = všechny 4 .glb existují."""
    pano = expected_glb(slug, "panorama")
    if not pano.exists():
        return "missing" if not pano.parent.exists() else "partial"
    for step in ("outer", "closeup", "inner"):
        if not expected_glb(slug, step).exists():
            return "partial"
    return "ready"


def _read_label(slug: str) -> str:
    """Zkusit přečíst manifest.json a vrátit region.label. Fallback = slug."""
    manifest = Path(f"{TILES_DIR_PREFIX}{slug}") / "manifest.json"
    if not manifest.exists():
        return slug
    try:
        data = json.loads(manifest.read_text())
        return data.get("region", {}).get("label") or slug
    except (json.JSONDecodeError, OSError):
        return slug


def list_locations() -> list[dict]:
    """Scan working dir pro 'tiles_v2_*' adresáře, vrátí list s
    {slug, label, status, has_panorama, has_outer, has_closeup, has_inner}."""
    out = []
    for path in sorted(Path(".").glob(f"{TILES_DIR_PREFIX}*")):
        if not path.is_dir():
            continue
        slug = path.name[len(TILES_DIR_PREFIX):]
        if not slug:
            continue
        out.append({
            "slug": slug,
            "label": _read_label(slug),
            "status": location_status(slug),
            "has_panorama": expected_glb(slug, "panorama").exists(),
            "has_outer":    expected_glb(slug, "outer").exists(),
            "has_closeup":  expected_glb(slug, "closeup").exists(),
            "has_inner":    expected_glb(slug, "inner").exists(),
        })
    return out
```

- [ ] **Step 3.4: Run tests, fix until green**

Run: `pytest tests/test_locations_unit.py -v`
Expected: vše PASS (23 + 5 = 28 tests).

- [ ] **Step 3.5: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): disk scan (list_locations, status, expected_glb)"
```

---

## Task 4: RUIAN search wrapper

**Files:**
- Modify: `locations.py`
- Modify: `tests/test_locations_unit.py`

Pozn.: RUIAN je externí síťová služba. Unit testy mockují `urllib.request.urlopen`. Skutečnou síťovou cestu ověříme manuálně v Task 13.

- [ ] **Step 4.1: Failing testy s mockem urlopen**

Připojit:

```python
from unittest.mock import patch, MagicMock
import io


def _mock_ruian_response(features):
    """Vyrobí MagicMock context manager s .read() vracejícím JSON."""
    payload = json.dumps({"features": features}).encode()
    cm = MagicMock()
    cm.__enter__ = MagicMock(return_value=MagicMock(read=lambda: payload))
    cm.__exit__ = MagicMock(return_value=False)
    return cm


def test_ruian_search_maps_features():
    fake_features = [
        {"attributes": {"adresa": "č.p. 136, 78501 Hnojice", "psc": 78501},
         "geometry": {"x": -547980.76, "y": -1107944.18}},
        {"attributes": {"adresa": "Jemnice 8, 59253 Strážek", "psc": 59253},
         "geometry": {"x": -626541.82, "y": -1130200.0}},
    ]
    with patch("locations.urlopen", return_value=_mock_ruian_response(fake_features)):
        result = locations.ruian_search("Hnojice")
    assert len(result) == 2
    assert result[0] == {
        "label": "č.p. 136, 78501 Hnojice",
        "sjtsk_cx": -547980.76,
        "sjtsk_cy": -1107944.18,
        "obec": "Hnojice",
    }
    assert result[1]["obec"] == "Strážek"


def test_ruian_search_empty():
    with patch("locations.urlopen", return_value=_mock_ruian_response([])):
        result = locations.ruian_search("nonsense")
    assert result == []


def test_ruian_search_escapes_quotes_and_percent():
    """Single quote a procento v dotazu musí být escapované, jinak SQL break v LIKE."""
    captured_url = []

    def fake_urlopen(req, timeout=None):
        captured_url.append(req.get_full_url() if hasattr(req, "get_full_url") else req)
        return _mock_ruian_response([])

    with patch("locations.urlopen", side_effect=fake_urlopen):
        locations.ruian_search("O'Hara 100%")

    sent = captured_url[0]
    # ' → '' a % escape: zkontrolujeme, že request URL neobsahuje literal "O'Hara"
    # ale escapovanou verzi
    assert "O%27%27Hara" in sent or "O''Hara" in sent  # url-encoded ''
    # Procento musí být v LIKE escapnuté (zde \\%)


def test_ruian_search_network_error_raises():
    from urllib.error import URLError
    with patch("locations.urlopen", side_effect=URLError("connection refused")):
        with pytest.raises(locations.RuianUnavailable):
            locations.ruian_search("Hnojice")
```

- [ ] **Step 4.2: Run, verify failures**

Run: `pytest tests/test_locations_unit.py -k ruian -v`
Expected: 4 FAIL (AttributeError: ruian_search / RuianUnavailable).

- [ ] **Step 4.3: Implementace**

Připojit do `locations.py`:

```python
import urllib.parse
from urllib.request import Request, urlopen
from urllib.error import URLError, HTTPError

RUIAN_ADRESNI_MISTO_URL = "https://ags.cuzk.cz/arcgis/rest/services/RUIAN/MapServer/1/query"


class RuianUnavailable(Exception):
    """ČÚZK RUIAN AdresniMisto service nedostupný (network / 5xx)."""


def _escape_like(q: str) -> str:
    """Escapuje single-quote (SQL injection) a procento/podtržítko (LIKE wildcardy)."""
    return q.replace("'", "''").replace("%", r"\%").replace("_", r"\_")


def ruian_search(q: str) -> list[dict]:
    """Volá ČÚZK RUIAN AdresniMisto LIKE query. Vrací list {label, sjtsk_cx,
    sjtsk_cy, obec}. Empty list = 0 hits. RuianUnavailable = network/5xx."""
    if not q.strip():
        return []
    escaped = _escape_like(q.strip())
    params = {
        "where": f"UPPER(adresa) LIKE UPPER('%{escaped}%')",
        "outFields": "adresa,psc,cislodomovni,kod",
        "outSR": "5514",
        "returnGeometry": "true",
        "resultRecordCount": "10",
        "f": "json",
    }
    url = RUIAN_ADRESNI_MISTO_URL + "?" + urllib.parse.urlencode(params)
    req = Request(url, headers={"User-Agent": "inzerator/1.0"})
    try:
        with urlopen(req, timeout=15) as resp:
            data = json.loads(resp.read())
    except (URLError, HTTPError, TimeoutError, OSError) as e:
        raise RuianUnavailable(str(e)) from e

    out = []
    for feat in data.get("features", []):
        attrs = feat.get("attributes", {})
        geom = feat.get("geometry", {})
        adresa = attrs.get("adresa", "")
        if not adresa or "x" not in geom or "y" not in geom:
            continue
        out.append({
            "label": adresa,
            "sjtsk_cx": float(geom["x"]),
            "sjtsk_cy": float(geom["y"]),
            "obec": parse_obec(adresa),
        })
    return out
```

- [ ] **Step 4.4: Run tests, fix until green**

Run: `pytest tests/test_locations_unit.py -k ruian -v`
Expected: 4 PASS.

Pokud `test_ruian_search_escapes_quotes_and_percent` selže (např. assertion na escape format): pamatuj, že `urlencode` zakóduje `''` na `%27%27`, takže obě podmínky v `assert` jsou ekvivalentní.

- [ ] **Step 4.5: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): ruian_search (AdresniMisto LIKE wrapper)"
```

---

## Task 5: Job state + enqueue

**Files:**
- Modify: `locations.py`
- Modify: `tests/test_locations_unit.py`

- [ ] **Step 5.1: Failing testy pro enqueue/get_job/list_active**

Připojit:

```python
@pytest.fixture
def clean_jobs():
    """Reset job state mezi testy."""
    with locations.JOB_LOCK:
        locations.JOBS.clear()
        locations.JOB_QUEUE.clear()
        locations.CURRENT_JOB = None
    yield
    with locations.JOB_LOCK:
        locations.JOBS.clear()
        locations.JOB_QUEUE.clear()
        locations.CURRENT_JOB = None


def test_enqueue_creates_job_with_4_steps(clean_jobs):
    job_id = locations.enqueue_job("test1", "Test Village", -500000.0, -1100000.0)
    assert job_id in locations.JOBS
    job = locations.JOBS[job_id]
    assert job["slug"] == "test1"
    assert job["label"] == "Test Village"
    assert job["cx"] == -500000.0
    assert job["cy"] == -1100000.0
    assert [s["name"] for s in job["steps"]] == list(locations.STEP_NAMES)
    assert all(s["state"] == "pending" for s in job["steps"])
    assert locations.JOB_QUEUE == [job_id]


def test_enqueue_two_jobs_fifo(clean_jobs):
    a = locations.enqueue_job("a", "A", -500000.0, -1100000.0)
    b = locations.enqueue_job("b", "B", -500000.0, -1100000.0)
    assert locations.JOB_QUEUE == [a, b]


def test_get_job_returns_none_for_unknown(clean_jobs):
    assert locations.get_job("nonexistent-uuid") is None


def test_list_active_jobs_queued(clean_jobs):
    a = locations.enqueue_job("a", "A", -500000.0, -1100000.0)
    b = locations.enqueue_job("b", "B", -500000.0, -1100000.0)
    active = locations.list_active_jobs()
    assert len(active) == 2
    assert active[0]["job_id"] == a
    assert active[0]["queue_position"] == 0
    assert active[1]["job_id"] == b
    assert active[1]["queue_position"] == 1
```

- [ ] **Step 5.2: Run, verify failures**

Run: `pytest tests/test_locations_unit.py -k "enqueue or get_job or list_active" -v`
Expected: 4 FAIL (AttributeError).

- [ ] **Step 5.3: Implementace**

Připojit do `locations.py`:

```python
import uuid
import time


def _new_job(slug: str, label: str, cx: float, cy: float) -> dict:
    return {
        "job_id": str(uuid.uuid4()),
        "slug": slug,
        "label": label,
        "cx": cx,
        "cy": cy,
        "cancelled": False,
        "created_at": time.time(),
        "steps": [
            {"name": name, "state": "pending", "error": None,
             "started_at": None, "finished_at": None}
            for name in STEP_NAMES
        ],
    }


def enqueue_job(slug: str, label: str, cx: float, cy: float) -> str:
    """Přidá nový job do JOBS + JOB_QUEUE. Vrátí job_id.
    Neověřuje slug-collision — to volá vyšší vrstva (HTTP handler)."""
    job = _new_job(slug, label, cx, cy)
    with JOB_CV:
        JOBS[job["job_id"]] = job
        JOB_QUEUE.append(job["job_id"])
        JOB_CV.notify()   # vzbudí worker
    return job["job_id"]


def get_job(job_id: str) -> dict | None:
    return JOBS.get(job_id)


def list_active_jobs() -> list[dict]:
    """Vrátí všechny joby, které jsou v queue nebo právě běží
    (CURRENT_JOB). Každá položka má queue_position (0 = right now / next)."""
    with JOB_LOCK:
        out = []
        # Currently running first (queue_position = -1 nebo 0 podle konvence)
        if CURRENT_JOB and CURRENT_JOB in JOBS:
            job = JOBS[CURRENT_JOB]
            out.append({**job, "queue_position": -1})
        for i, jid in enumerate(JOB_QUEUE):
            if jid in JOBS:
                out.append({**JOBS[jid], "queue_position": i})
        return out
```

- [ ] **Step 5.4: Run tests**

Run: `pytest tests/test_locations_unit.py -k "enqueue or get_job or list_active" -v`
Expected: 4 PASS.

Pozn.: `test_list_active_jobs_queued` očekává `queue_position` 0 a 1 pro 2 queued joby (žádný running). Implementace pro queued vrací enumerate index, takže 0 a 1 ✓.

- [ ] **Step 5.5: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): job state + enqueue/get/list-active"
```

---

## Task 6: Retry + cancel

**Files:**
- Modify: `locations.py`
- Modify: `tests/test_locations_unit.py`

- [ ] **Step 6.1: Failing testy**

Připojit:

```python
def test_retry_resets_failed_steps(clean_jobs):
    """Retry odebere `fail` stepy zpět na `pending` a vrátí job_id do fronty."""
    job_id = locations.enqueue_job("t", "T", 0.0, 0.0)
    # Simuluj že job už proběhl a closeup selhal:
    locations.JOB_QUEUE.clear()
    job = locations.JOBS[job_id]
    job["steps"][0]["state"] = "ok"      # panorama
    job["steps"][1]["state"] = "ok"      # outer
    job["steps"][2]["state"] = "fail"    # closeup
    job["steps"][2]["error"] = "test error"
    job["steps"][3]["state"] = "pending" # inner stayed
    assert locations.retry_job(job_id) is True
    assert locations.JOB_QUEUE == [job_id]
    # Failed se reset; ok zůstane (worker je preskočí přes existenci .glb)
    assert job["steps"][2]["state"] == "pending"
    assert job["steps"][2]["error"] is None
    assert job["steps"][0]["state"] == "ok"   # ok zůstane


def test_retry_unknown_job_returns_false(clean_jobs):
    assert locations.retry_job("nonexistent") is False


def test_cancel_queued_removes_from_queue(clean_jobs):
    a = locations.enqueue_job("a", "A", 0.0, 0.0)
    b = locations.enqueue_job("b", "B", 0.0, 0.0)
    assert locations.cancel_job(a) is True
    assert locations.JOB_QUEUE == [b]
    assert locations.JOBS[a]["cancelled"] is True


def test_cancel_already_done_returns_false(clean_jobs):
    job_id = locations.enqueue_job("t", "T", 0.0, 0.0)
    locations.JOB_QUEUE.clear()
    for s in locations.JOBS[job_id]["steps"]:
        s["state"] = "ok"
    assert locations.cancel_job(job_id) is False
```

- [ ] **Step 6.2: Run, verify failures**

Run: `pytest tests/test_locations_unit.py -k "retry or cancel" -v`
Expected: 4 FAIL.

- [ ] **Step 6.3: Implementace**

Připojit do `locations.py`:

```python
def retry_job(job_id: str) -> bool:
    """Reset `fail` stepy zpět na `pending`, znovu zařadit do fronty.
    Worker resume-skipne hotové stepy (existence .glb na disku).
    Vrátí True pokud nalezeno, False pokud job_id neznámé."""
    with JOB_CV:
        job = JOBS.get(job_id)
        if job is None:
            return False
        # Neretrujeme job, který je právě běžící (CURRENT_JOB)
        if CURRENT_JOB == job_id:
            return False
        if job_id in JOB_QUEUE:
            return True   # už je ve frontě, idempotent
        for step in job["steps"]:
            if step["state"] in ("fail", "cancelled"):
                step["state"] = "pending"
                step["error"] = None
                step["started_at"] = None
                step["finished_at"] = None
        job["cancelled"] = False
        JOB_QUEUE.append(job_id)
        JOB_CV.notify()
        return True


def cancel_job(job_id: str) -> bool:
    """Zruší job. Pokud queued: vyhodí z fronty. Pokud running: nastaví
    `cancelled = True`, worker zabije proces po dokončení aktuálního I/O
    polling cyklu (max 10 s po terminate(), pak kill()).
    Vrátí False pokud job už hotový (ok / fail final state)."""
    with JOB_LOCK:
        job = JOBS.get(job_id)
        if job is None:
            return False
        if all(s["state"] in ("ok", "fail", "cancelled", "skipped") for s in job["steps"]):
            return False   # už hotový
        job["cancelled"] = True
        if job_id in JOB_QUEUE:
            JOB_QUEUE.remove(job_id)
        # Pokud běží: worker si všimne `cancelled` na další iteraci.
        # Skutečný subprocess.terminate() jde z worker threadu (CURRENT_PROC).
        global _CANCEL_REQUESTED
        if CURRENT_JOB == job_id:
            _CANCEL_REQUESTED = True
        return True


_CANCEL_REQUESTED = False
```

Pozn.: skutečný kill subprocesu se implementuje v Tasku 7 (worker thread má referenci na `CURRENT_PROC`). V tomto tasku jen flagujeme `cancelled = True` a `_CANCEL_REQUESTED`.

- [ ] **Step 6.4: Run tests**

Run: `pytest tests/test_locations_unit.py -k "retry or cancel" -v`
Expected: 4 PASS.

- [ ] **Step 6.5: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): retry + cancel (state-only, worker bridge in Task 7)"
```

---

## Task 7: Worker thread + subprocess pipeline

**Files:**
- Modify: `locations.py`
- Modify: `tests/test_locations_unit.py`

- [ ] **Step 7.1: Failing testy s mocked subprocess**

Připojit:

```python
import os


def test_cmd_for_panorama():
    cmd = locations.cmd_for("panorama", "test", -547700.0, -1107700.0)
    assert cmd[0] == "python3"
    assert "gen_panorama.py" in cmd[1]
    assert "--region" in cmd
    assert "test" in cmd
    # --center-sjtsk používá `=` syntax kvůli negativním číslům + argparse
    assert any(arg.startswith("--center-sjtsk=") for arg in cmd)


def test_cmd_for_inner_has_step_0_5():
    """Inner detail = --step 0.5 (jemnější než stávající Hnojice manifest)."""
    cmd = locations.cmd_for("inner", "test", -547700.0, -1107700.0)
    cmd_str = " ".join(cmd)
    assert "--slug inner" in cmd_str
    assert "--step 0.5" in cmd_str
    assert "--half 500" in cmd_str
    assert "--fade-to closeup" in cmd_str


def test_cmd_for_outer_closeup_steps():
    outer = " ".join(locations.cmd_for("outer", "x", 0.0, 0.0))
    closeup = " ".join(locations.cmd_for("closeup", "x", 0.0, 0.0))
    assert "--half 2500" in outer and "--step 2.5" in outer and "--fade-to panorama" in outer
    assert "--half 1500" in closeup and "--step 1.5" in closeup and "--fade-to outer" in closeup


def test_worker_runs_job_to_completion(tmp_path, monkeypatch, clean_jobs):
    """Mock subprocess.Popen, aby vytvořil expected .glb. Worker projde
    všechny 4 stepy → state ok pro každý."""
    monkeypatch.chdir(tmp_path)

    class FakeProc:
        def __init__(self, cmd, **kw):
            self.cmd = cmd
            self.returncode = 0
        def communicate(self, timeout=None):
            # Najdi --region a --slug v cmd, vytvoř expected .glb
            args = self.cmd
            region = args[args.index("--region") + 1]
            slug_step = "panorama"
            if "--slug" in args:
                slug_step = args[args.index("--slug") + 1]
            out_path = locations.expected_glb(region, slug_step)
            out_path.parent.mkdir(parents=True, exist_ok=True)
            out_path.touch()
            return "fake stdout", ""
        def terminate(self): pass
        def kill(self): pass
        def wait(self, timeout=None): pass

    monkeypatch.setattr(locations.subprocess, "Popen", FakeProc)

    job_id = locations.enqueue_job("test", "T", 0.0, 0.0)
    # Spustit worker jednou (single-iteration helper)
    locations._run_one_job_for_test()
    job = locations.JOBS[job_id]
    assert all(s["state"] == "ok" for s in job["steps"]), \
        f"states: {[(s['name'], s['state']) for s in job['steps']]}"


def test_worker_skips_existing_glb(tmp_path, monkeypatch, clean_jobs):
    """Pokud panorama.glb už existuje, step → skipped, ne running."""
    monkeypatch.chdir(tmp_path)
    (tmp_path / "tiles_v2_test").mkdir()
    (tmp_path / "tiles_v2_test" / "panorama.glb").touch()

    class FakeProc:
        def __init__(self, cmd, **kw):
            self.cmd = cmd
            self.returncode = 0
        def communicate(self, timeout=None):
            args = self.cmd
            region = args[args.index("--region") + 1]
            slug_step = args[args.index("--slug") + 1] if "--slug" in args else "panorama"
            out_path = locations.expected_glb(region, slug_step)
            out_path.parent.mkdir(parents=True, exist_ok=True)
            out_path.touch()
            return "", ""
        def terminate(self): pass
        def kill(self): pass
        def wait(self, timeout=None): pass

    monkeypatch.setattr(locations.subprocess, "Popen", FakeProc)
    job_id = locations.enqueue_job("test", "T", 0.0, 0.0)
    locations._run_one_job_for_test()
    job = locations.JOBS[job_id]
    assert job["steps"][0]["state"] == "skipped"
    assert all(s["state"] == "ok" for s in job["steps"][1:])


def test_worker_failure_stops_loop(tmp_path, monkeypatch, clean_jobs):
    """Pokud subprocess vrátí non-zero, step → fail, loop break,
    zbývající stepy zůstanou pending."""
    monkeypatch.chdir(tmp_path)

    class FailingProc:
        def __init__(self, cmd, **kw):
            self.cmd = cmd
            self.returncode = 1
        def communicate(self, timeout=None):
            return "", "Traceback (most recent call last):\n  File 'foo'\nValueError: bad"
        def terminate(self): pass
        def kill(self): pass
        def wait(self, timeout=None): pass

    monkeypatch.setattr(locations.subprocess, "Popen", FailingProc)
    job_id = locations.enqueue_job("test", "T", 0.0, 0.0)
    locations._run_one_job_for_test()
    job = locations.JOBS[job_id]
    assert job["steps"][0]["state"] == "fail"
    assert "ValueError" in job["steps"][0]["error"]
    assert all(s["state"] == "pending" for s in job["steps"][1:])
```

- [ ] **Step 7.2: Run, verify failures**

Run: `pytest tests/test_locations_unit.py -k "cmd_for or worker_runs or worker_skips or worker_failure" -v`
Expected: vše FAIL.

- [ ] **Step 7.3: Implementace worker + cmd_for**

Připojit do `locations.py`:

```python
import subprocess

CURRENT_PROC: subprocess.Popen | None = None
_WORKER_THREAD: threading.Thread | None = None

# Hnojice-preset LOD parametry (fixní pro všechny lokace).
# Inner step 0.5 m kvůli zarovnání s gen_multitile village_flat profilem.
_LOD_PRESET = {
    "outer":   {"half": 2500, "step": "2.5", "fade": 100, "fade_to": "panorama", "zoom": 17, "size": 4096},
    "closeup": {"half": 1500, "step": "1.5", "fade":  50, "fade_to": "outer",    "zoom": 21, "size": 8192},
    "inner":   {"half":  500, "step": "0.5", "fade":  30, "fade_to": "closeup",  "zoom": 21, "size": 8192},
}


def cmd_for(step: str, slug: str, cx: float, cy: float) -> list[str]:
    """Vyrobí subprocess command pro daný step. `--center-sjtsk=cx,cy`
    musí mít `=` syntax (argparse jinak parsuje negativní cx jako flag)."""
    base = ["python3"]
    center = f"--center-sjtsk={cx},{cy}"
    if step == "panorama":
        return base + ["gen_panorama.py", "--region", slug, center]
    p = _LOD_PRESET[step]
    return base + [
        "gen_detail.py",
        "--region", slug,
        "--slug", step,
        center,
        "--half", str(p["half"]),
        "--step", p["step"],
        "--fade", str(p["fade"]),
        "--fade-to", p["fade_to"],
        "--zoom", str(p["zoom"]),
        "--size", str(p["size"]),
    ]


def _format_error(stderr: str) -> str:
    """Pythonský traceback má informaci na začátku (file/line) i na konci
    (exception class + message). Vrátíme head 300 + tail 300, pokud > 600."""
    if len(stderr) <= 600:
        return stderr
    return stderr[:300] + "\n... [truncated] ...\n" + stderr[-300:]


def _run_step(job: dict, step: dict) -> bool:
    """Spustí jeden step jako subprocess. Vrátí True pokud ok/skipped,
    False pokud fail/cancelled (= worker má break)."""
    global CURRENT_PROC
    expected = expected_glb(job["slug"], step["name"])
    if expected.exists():
        step["state"] = "skipped"
        return True

    step["state"] = "running"
    step["started_at"] = time.time()
    cmd = cmd_for(step["name"], job["slug"], job["cx"], job["cy"])
    log_dir = JOB_LOG_DIR / job["job_id"]
    log_dir.mkdir(parents=True, exist_ok=True)
    log_path = log_dir / f"{step['name']}.log"

    try:
        CURRENT_PROC = subprocess.Popen(
            cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            text=True,
        )
    except OSError as e:
        step["state"] = "fail"
        step["error"] = f"spawn failed: {e}"
        return False

    try:
        out, err = CURRENT_PROC.communicate(timeout=STEP_TIMEOUT_SECS)
    except subprocess.TimeoutExpired:
        CURRENT_PROC.kill()
        out, err = CURRENT_PROC.communicate()
        log_path.write_text((out or "") + "\n--- STDERR ---\n" + (err or ""))
        step["state"] = "fail"
        step["error"] = f"timeout {STEP_TIMEOUT_SECS // 60}m"
        step["finished_at"] = time.time()
        return False

    log_path.write_text((out or "") + "\n--- STDERR ---\n" + (err or ""))
    step["finished_at"] = time.time()

    if job["cancelled"]:
        step["state"] = "cancelled"
        return False
    if CURRENT_PROC.returncode == 0 and expected.exists():
        step["state"] = "ok"
        return True
    step["state"] = "fail"
    step["error"] = _format_error(err or f"exit {CURRENT_PROC.returncode}, .glb missing")
    return False


def _run_one_job(job_id: str) -> None:
    """Zpracuje jeden job ze začátku do konce / do fail/cancel.
    Volá worker_loop i test helper."""
    global CURRENT_JOB, CURRENT_PROC, _CANCEL_REQUESTED
    job = JOBS.get(job_id)
    if job is None or job["cancelled"]:
        return
    CURRENT_JOB = job_id
    CURRENT_PROC = None
    _CANCEL_REQUESTED = False
    try:
        for step in job["steps"]:
            if job["cancelled"]:
                step["state"] = "cancelled"
                break
            if not _run_step(job, step):
                break
    finally:
        CURRENT_JOB = None
        CURRENT_PROC = None


def _run_one_job_for_test() -> None:
    """Helper: spotřebuje 1 job z fronty + zpracuje sériově (bez threadingu).
    Používá se v unit testech."""
    with JOB_LOCK:
        if not JOB_QUEUE:
            return
        job_id = JOB_QUEUE.pop(0)
    _run_one_job(job_id)


def worker_loop() -> None:
    """Background thread: čeká na frontu, spotřebuje joby."""
    while True:
        with JOB_CV:
            while not JOB_QUEUE:
                JOB_CV.wait()
            job_id = JOB_QUEUE.pop(0)
        _run_one_job(job_id)


def start_worker() -> threading.Thread:
    """Spustí worker thread jako daemon. Vrátí Thread objekt (idempotent)."""
    global _WORKER_THREAD
    if _WORKER_THREAD is not None and _WORKER_THREAD.is_alive():
        return _WORKER_THREAD
    _WORKER_THREAD = threading.Thread(target=worker_loop, daemon=True, name="job-worker")
    _WORKER_THREAD.start()
    return _WORKER_THREAD
```

A nahoře v souboru přidat `subprocess` import (pokud ještě není).

- [ ] **Step 7.4: Cancel s běžícím procesem — dopracovat `cancel_job`**

V `cancel_job` u case `CURRENT_JOB == job_id`, přidat skutečné `terminate`:

```python
def cancel_job(job_id: str) -> bool:
    with JOB_LOCK:
        job = JOBS.get(job_id)
        if job is None:
            return False
        if all(s["state"] in ("ok", "fail", "cancelled", "skipped") for s in job["steps"]):
            return False
        job["cancelled"] = True
        if job_id in JOB_QUEUE:
            JOB_QUEUE.remove(job_id)
        if CURRENT_JOB == job_id and CURRENT_PROC is not None:
            try:
                CURRENT_PROC.terminate()
                # 10 s grace, pak kill
                threading.Timer(10.0, lambda: CURRENT_PROC.kill() if CURRENT_PROC and CURRENT_PROC.poll() is None else None).start()
            except (ProcessLookupError, AttributeError):
                pass
        return True
```

- [ ] **Step 7.5: Run all unit tests**

Run: `pytest tests/test_locations_unit.py -v`
Expected: vše PASS.

Pokud `test_worker_runs_job_to_completion` selže s něčím o `JOB_LOG_DIR`, ověř že tempdir má povolené vytváření `cache/jobs/<id>/`. `JOB_LOG_DIR = Path("cache/jobs")` se vyhodnocuje za běhu relativně k `cwd`, takže `monkeypatch.chdir(tmp_path)` to ošetří.

- [ ] **Step 7.6: Commit**

```bash
git add locations.py tests/test_locations_unit.py
git commit -m "feat(locations): worker thread + subprocess pipeline (inner step 0.5)"
```

---

## Task 8: server.py integration — do_POST + /api/* routing

**Files:**
- Modify: `server.py`
- Create: `tests/test_locations_api.py`

- [ ] **Step 8.1: Failing integration test (server musí běžet)**

```python
# tests/test_locations_api.py
"""Integration testy proti běžícímu serveru na :8080.
Tyto testy předpokládají, že server.py běží — spustit:
    python3 server.py &

Cleanup: testy používají test-only slug 'test-' prefix, takže nezasahují
do skutečných lokací. Po skončení manuálně:
    rm -rf tiles_v2_test-* cache/jobs/
"""
import json
import time
import urllib.request
import urllib.error

BASE = "http://127.0.0.1:8080"


def _get(path):
    with urllib.request.urlopen(f"{BASE}{path}", timeout=15) as r:
        return r.status, json.loads(r.read())


def _post(path, body):
    req = urllib.request.Request(
        f"{BASE}{path}",
        data=json.dumps(body).encode(),
        headers={"Content-Type": "application/json"},
        method="POST",
    )
    try:
        with urllib.request.urlopen(req, timeout=15) as r:
            return r.status, json.loads(r.read()) if r.length else {}
    except urllib.error.HTTPError as e:
        return e.code, json.loads(e.read()) if e.length else {}


def test_locations_endpoint_returns_list():
    status, data = _get("/api/locations")
    assert status == 200
    assert isinstance(data, list)


def test_create_job_invalid_slug_400():
    status, _ = _post("/api/jobs", {
        "slug": "Bad Slug!", "label": "X", "cx": -500000.0, "cy": -1100000.0,
    })
    assert status == 400


def test_create_job_missing_params_400():
    status, _ = _post("/api/jobs", {"slug": "test-ok"})
    assert status == 400


def test_ruian_search_empty_query_returns_empty():
    status, data = _get("/api/ruian/search?q=")
    assert status == 200
    assert data == []
```

- [ ] **Step 8.2: Implementovat handler v `server.py`**

V `server.py`, importovat na začátek souboru (poblíž ostatních importů):

```python
import locations
```

Najít `class ProxyHandler(http.server.SimpleHTTPRequestHandler):` (kolem řádku 600 dle git diff) a přidat tyto metody (vložit poblíž ostatních `do_GET` handlers, např. na konec class):

```python
    def _send_json(self, status: int, payload):
        body = json.dumps(payload).encode()
        self.send_response(status)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(body)))
        self.send_header("Access-Control-Allow-Origin", "*")
        self.end_headers()
        self.wfile.write(body)

    def _read_json_body(self):
        length = int(self.headers.get("Content-Length", "0"))
        if length == 0:
            return {}
        raw = self.rfile.read(length).decode("utf-8")
        return json.loads(raw)

    def do_POST(self):
        parsed = urllib.parse.urlparse(self.path)
        path = parsed.path
        try:
            if path == "/api/jobs":
                self._handle_post_jobs()
            elif path.startswith("/api/jobs/") and path.endswith("/retry"):
                self._handle_post_jobs_retry(path[len("/api/jobs/"):-len("/retry")])
            elif path.startswith("/api/jobs/") and path.endswith("/cancel"):
                self._handle_post_jobs_cancel(path[len("/api/jobs/"):-len("/cancel")])
            else:
                self.send_error(404, "POST endpoint not found")
        except json.JSONDecodeError:
            self._send_json(400, {"error": "invalid JSON body"})
        except Exception as e:
            import traceback
            traceback.print_exc()
            self._send_json(500, {"error": str(e)})

    def _handle_post_jobs(self):
        body = self._read_json_body()
        slug = body.get("slug", "").strip()
        label = body.get("label", slug).strip() or slug
        cx = body.get("cx")
        cy = body.get("cy")
        if not slug or cx is None or cy is None:
            self._send_json(400, {"error": "required fields: slug, cx, cy"})
            return
        if not locations.is_valid_slug(slug):
            self._send_json(400, {"error": "slug must match ^[a-z0-9-]+$"})
            return
        try:
            cx = float(cx); cy = float(cy)
        except (TypeError, ValueError):
            self._send_json(400, {"error": "cx, cy must be numbers"})
            return
        status = locations.location_status(slug)
        if status == "ready":
            self._send_json(409, {
                "error": "slug already exists and is ready",
                "slug": slug,
                "suggestion": locations.next_free_slug(slug,
                    set(loc["slug"] for loc in locations.list_locations())),
            })
            return
        # 'partial' nebo 'missing' = OK, worker resume-skipne hotové stepy
        job_id = locations.enqueue_job(slug, label, cx, cy)
        self._send_json(200, {"job_id": job_id, "slug": slug})

    def _handle_post_jobs_retry(self, job_id):
        ok = locations.retry_job(job_id)
        self._send_json(200 if ok else 404,
            {"job_id": job_id, "retried": ok} if ok else {"error": "job not found"})

    def _handle_post_jobs_cancel(self, job_id):
        ok = locations.cancel_job(job_id)
        self._send_json(200 if ok else 404,
            {"job_id": job_id, "cancelled": ok} if ok else {"error": "job not found or already done"})
```

A do existujícího `do_GET` přidat routing pro 4 nové GET endpointy. Najít na začátku `do_GET` (kolem řádku ~620 v rozšířené verzi) nebo poblíž první `if`:

```python
        # API endpoints (nová Lokace UI)
        if path == "/api/locations":
            self._send_json(200, locations.list_locations())
            return
        if path == "/api/ruian/search":
            q = query.get("q", [""])[0]
            try:
                results = locations.ruian_search(q)
            except locations.RuianUnavailable as e:
                self._send_json(503, {"error": f"ČÚZK unavailable: {e}"})
                return
            self._send_json(200, results)
            return
        if path == "/api/jobs":
            if query.get("active", [None])[0] == "1":
                self._send_json(200, locations.list_active_jobs())
            else:
                self._send_json(200, locations.list_active_jobs())
            return
        if path.startswith("/api/jobs/"):
            job_id = path[len("/api/jobs/"):]
            job = locations.get_job(job_id)
            if job is None:
                self._send_json(404, {"error": "job not found"})
                return
            self._send_json(200, job)
            return
```

(Přesné místo vložení záleží na aktuálním tvaru `do_GET` — vložit hned za parse `parsed = urllib.parse.urlparse(self.path); path = parsed.path; query = urllib.parse.parse_qs(parsed.query)`, před existující `/proxy/*` routing.)

- [ ] **Step 8.3: V `if __name__ == "__main__":` spustit worker**

V `server.py` na konci (kolem řádku 2295):

```python
if __name__ == "__main__":
    locations.start_worker()
    server = http.server.ThreadingHTTPServer(("0.0.0.0", PORT), ProxyHandler)
    print(f"Server: http://0.0.0.0:{PORT}/ (threaded, job worker started)")
    server.serve_forever()
```

- [ ] **Step 8.4: Restart server + spustit integration testy**

```bash
# Najít a zabít existující python3 server.py běh
lsof -ti:8080 | xargs -r kill
python3 server.py > /tmp/inzerator-server.log 2>&1 &
sleep 2
pytest tests/test_locations_api.py -v
```

Expected: 4 PASS.

Pokud `test_ruian_search_empty_query_returns_empty` selže, zkontrolovat, že `locations.ruian_search("")` opravdu vrátí `[]` bez síťového volání (řádek `if not q.strip(): return []` v Tasku 4).

- [ ] **Step 8.5: Commit**

```bash
git add server.py tests/test_locations_api.py
git commit -m "feat(server): /api/locations + /api/ruian/search + /api/jobs endpoints"
```

---

## Task 9: index.html — dashboard skeleton (read-only)

**Files:**
- Create: `index.html`

- [ ] **Step 9.1: Vytvořit `index.html` s prázdným seznamem + fetch**

```html
<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Inženýrátor — Lokace</title>
  <style>
    body { font-family: -apple-system, "Helvetica Neue", sans-serif;
           background: #f5f5f7; color: #1d1d1f; max-width: 800px;
           margin: 24px auto; padding: 0 16px; }
    h1 { font-size: 22px; margin: 0 0 16px 0; }
    .location-row { display: flex; align-items: center; gap: 12px;
                    padding: 12px 14px; background: white;
                    border-radius: 8px; margin-bottom: 8px;
                    box-shadow: 0 1px 2px rgba(0,0,0,0.06); }
    .location-row .label { flex: 1; font-weight: 500; }
    .location-row .status { font-size: 13px; color: #666; }
    .status-ready { color: #2a8a2a; }
    .status-partial { color: #c08000; }
    .status-generating { color: #1a73e8; }
    .btn { padding: 6px 12px; border: 0; border-radius: 6px;
           background: #1a73e8; color: white; cursor: pointer;
           font-size: 13px; }
    .btn-secondary { background: #e0e0e0; color: #333; }
    .btn:disabled { opacity: 0.4; cursor: default; }
    .empty { padding: 24px; text-align: center; color: #999; }
    .error { color: #c30; padding: 12px; background: #fff0f0;
             border-radius: 6px; margin: 8px 0; }
  </style>
</head>
<body>
  <h1>Lokace</h1>
  <div id="locations"><div class="empty">Načítám…</div></div>
  <div style="margin-top: 24px;">
    <button class="btn" id="new-location-btn">+ Nová lokace</button>
  </div>
  <div id="form-container"></div>

  <script>
  // === Renders ===
  async function renderLocations() {
    const r = await fetch('/api/locations').then(r => r.json()).catch(e => null);
    const el = document.getElementById('locations');
    if (!r) {
      el.innerHTML = '<div class="error">Backend nedostupný</div>';
      return;
    }
    if (r.length === 0) {
      el.innerHTML = '<div class="empty">Žádné lokace. Klikni „+ Nová lokace".</div>';
      return;
    }
    el.innerHTML = r.map(loc => `
      <div class="location-row" data-slug="${loc.slug}">
        <span class="label">${escapeHtml(loc.label)}</span>
        <span class="status status-${loc.status}">${statusLabel(loc)}</span>
        ${loc.status === 'ready'
          ? `<a class="btn" href="/v2.html?region=${loc.slug}">Otevřít</a>`
          : ''}
      </div>
    `).join('');
  }

  function statusLabel(loc) {
    if (loc.status === 'ready') return '✓ ready';
    if (loc.status === 'partial') return '⚠ partial';
    return loc.status;
  }

  function escapeHtml(s) {
    return String(s).replace(/[&<>"']/g,
      c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  }

  // Initial load
  renderLocations();
  </script>
</body>
</html>
```

- [ ] **Step 9.2: Manuální smoke test**

Otevři v prohlížeči (přes Tailscale URL):
- `https://jans-mac-mini.tailfe475e.ts.net/` → měl by ukázat existující Hnojici jako „✓ ready" + tlačítko „Otevřít"
- Klik „Otevřít" → otevře `v2.html?region=hnojice`

- [ ] **Step 9.3: Commit**

```bash
git add index.html
git commit -m "feat(index): dashboard skeleton (locations list, no form yet)"
```

---

## Task 10: index.html — formulář pro novou lokaci + RUIAN search

**Files:**
- Modify: `index.html`

- [ ] **Step 10.1: Toggle formulář + adresa search**

Najít v `index.html` `<div id="form-container"></div>` a celý `<script>` blok. Nahradit obsah `<script>` na (po `renderLocations` definici) tímhle rozšířením:

```html
  <script>
  // ... renderLocations() z předchozího ...

  let SELECTED_HIT = null;

  function renderForm(show) {
    const el = document.getElementById('form-container');
    if (!show) { el.innerHTML = ''; return; }
    el.innerHTML = `
      <div style="background:white; padding:16px; border-radius:8px; margin-top:16px;
                  box-shadow:0 1px 2px rgba(0,0,0,0.06);">
        <h3 style="margin-top:0;">Nová lokace</h3>
        <div>
          <input id="addr-input" type="text" placeholder="Hnojice 47 (s diakritikou!)"
                 style="width:60%; padding:6px;">
          <button class="btn" id="search-btn">Hledat</button>
        </div>
        <div id="search-results" style="margin-top:12px;"></div>
        <div id="selected-fields" style="margin-top:12px; display:none;">
          <p><b>Vybráno:</b> <span id="selected-label"></span></p>
          <label>Slug: <input id="slug-input" type="text"></label><br><br>
          <label>Label: <input id="label-input" type="text" style="width:60%"></label><br><br>
          <p style="font-size:12px; color:#666;">S-JTSK: <span id="sjtsk-display"></span></p>
          <button class="btn" id="generate-btn">Generovat</button>
          <button class="btn btn-secondary" id="cancel-form-btn">Zrušit</button>
        </div>
      </div>
    `;
    document.getElementById('search-btn').onclick = doSearch;
    document.getElementById('cancel-form-btn').onclick = () => renderForm(false);
    document.getElementById('addr-input').addEventListener('keydown', e => {
      if (e.key === 'Enter') doSearch();
    });
  }

  async function doSearch() {
    const q = document.getElementById('addr-input').value.trim();
    const resultsEl = document.getElementById('search-results');
    if (!q) {
      resultsEl.innerHTML = '<div class="error">Zadej alespoň 2 znaky</div>';
      return;
    }
    resultsEl.innerHTML = '<i>Hledám…</i>';
    try {
      const r = await fetch(`/api/ruian/search?q=${encodeURIComponent(q)}`);
      if (r.status === 503) {
        resultsEl.innerHTML = '<div class="error">ČÚZK nedostupné. Zkus později nebo zadej S-JTSK ručně.</div>';
        return;
      }
      const data = await r.json();
      if (data.length === 0) {
        resultsEl.innerHTML = '<div class="error">Nic nenalezeno. Zkus i s diakritikou (Strážek, ne Strazek).</div>';
        return;
      }
      resultsEl.innerHTML = data.map((hit, i) => `
        <div style="padding:8px; cursor:pointer; border-bottom:1px solid #eee;"
             data-i="${i}" class="hit-row">
          ${escapeHtml(hit.label)}
        </div>
      `).join('');
      // Klik na hit → prefill form
      resultsEl.querySelectorAll('.hit-row').forEach((el, i) => {
        el.onclick = () => selectHit(data[i]);
      });
    } catch (e) {
      resultsEl.innerHTML = `<div class="error">Chyba: ${escapeHtml(e.message)}</div>`;
    }
  }

  async function selectHit(hit) {
    SELECTED_HIT = hit;
    // Auto-slug: slugify(obec), kolize-check přes /api/locations
    const locs = await fetch('/api/locations').then(r => r.json());
    const existing = new Set(locs.map(l => l.slug));
    const base = slugifyJS(hit.obec);
    let slug = base;
    let n = 2;
    while (existing.has(slug)) { slug = `${base}-${n}`; n++; }

    document.getElementById('selected-label').textContent = hit.label;
    document.getElementById('slug-input').value = slug;
    document.getElementById('label-input').value = hit.label;
    document.getElementById('sjtsk-display').textContent =
      `${hit.sjtsk_cx.toFixed(2)}, ${hit.sjtsk_cy.toFixed(2)}`;
    document.getElementById('selected-fields').style.display = 'block';
    document.getElementById('generate-btn').onclick = doGenerate;
  }

  function slugifyJS(text) {
    return text
      .normalize('NFD').replace(/[̀-ͯ]/g, '')
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, '-')
      .replace(/^-+|-+$/g, '');
  }

  async function doGenerate() {
    if (!SELECTED_HIT) return;
    const body = {
      slug: document.getElementById('slug-input').value.trim(),
      label: document.getElementById('label-input').value.trim(),
      cx: SELECTED_HIT.sjtsk_cx,
      cy: SELECTED_HIT.sjtsk_cy,
    };
    const r = await fetch('/api/jobs', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify(body),
    });
    const data = await r.json();
    if (r.status !== 200) {
      alert(`Chyba ${r.status}: ${data.error || 'unknown'}` +
            (data.suggestion ? `\nZkus slug: ${data.suggestion}` : ''));
      return;
    }
    renderForm(false);
    SELECTED_HIT = null;
    await renderLocations();
    // V Task 11: spustit polling
  }

  document.getElementById('new-location-btn').onclick = () => renderForm(true);
  </script>
```

- [ ] **Step 10.2: Manuální smoke test**

V prohlížeči:
1. Klik [+ Nová lokace] → formulář se rozbalí
2. Napiš „Hnojice" + Enter → vidět seznam hitů
3. Napiš „Strazek" (bez diakritiky) → očekávat „Nic nenalezeno"
4. Napiš „Strážek" → vidět hity
5. Klik na hit → vyplní slug, label, S-JTSK
6. (Generovat ještě nebudeme, polling v Task 11)

- [ ] **Step 10.3: Commit**

```bash
git add index.html
git commit -m "feat(index): RUIAN search form + slug auto-fill"
```

---

## Task 11: index.html — polling + step status display

**Files:**
- Modify: `index.html`

- [ ] **Step 11.1: Přidat polling + per-job status rendering**

Do `<script>` v `index.html` přidat (před `renderLocations()` na konec script bloku):

```javascript
  let POLL_TIMER = null;
  let ACTIVE_JOBS = [];

  async function pollOnce() {
    try {
      const r = await fetch('/api/jobs?active=1');
      ACTIVE_JOBS = await r.json();
    } catch (e) {
      ACTIVE_JOBS = [];
    }
    renderActiveJobs();
    if (ACTIVE_JOBS.length > 0) {
      POLL_TIMER = setTimeout(pollOnce, 2000);
    } else {
      POLL_TIMER = null;
      await renderLocations();   // refresh dashboard po dokončení
    }
  }

  function startPolling() {
    if (POLL_TIMER === null) pollOnce();
  }

  function renderActiveJobs() {
    // Render runnng/queued joby jako přidané řádky pod existujícími lokacemi.
    // Pokud má lokace ve stejném slugu už řádku, nahradíme.
    const container = document.getElementById('locations');
    const existing = Array.from(container.querySelectorAll('.location-row'));
    const existingBySlug = new Map(existing.map(el => [el.dataset.slug, el]));

    for (const job of ACTIVE_JOBS) {
      const row = renderJobRow(job);
      const old = existingBySlug.get(job.slug);
      if (old) old.replaceWith(row);
      else container.appendChild(row);
    }
  }

  function renderJobRow(job) {
    const div = document.createElement('div');
    div.className = 'location-row';
    div.dataset.slug = job.slug;
    div.dataset.jobId = job.job_id;
    const stepIcons = job.steps.map(s => {
      const icon = {
        pending: '⏳', running: '⏳', ok: '✓', fail: '✗',
        skipped: '↷', cancelled: '⊘',
      }[s.state] || '?';
      const color = (s.state === 'fail' || s.state === 'cancelled') ? '#c30'
                  : (s.state === 'running') ? '#1a73e8' : '#666';
      return `<span style="color:${color}">${icon} ${s.name}</span>`;
    }).join(' · ');
    const failedStep = job.steps.find(s => s.state === 'fail');
    const isDone = job.steps.every(s =>
      ['ok','fail','cancelled','skipped'].includes(s.state));
    const hasFail = !!failedStep;

    div.innerHTML = `
      <span class="label">${escapeHtml(job.label || job.slug)}</span>
      <span class="status status-generating" style="font-size:12px">${stepIcons}</span>
      ${hasFail ? `<button class="btn" data-action="retry">Retry</button>` : ''}
      ${!isDone ? `<button class="btn btn-secondary" data-action="cancel">Cancel</button>` : ''}
    `;
    if (failedStep) {
      div.title = `${failedStep.name}: ${failedStep.error || 'unknown error'}`;
    }
    // Event handlers
    div.querySelector('[data-action="retry"]')?.addEventListener('click',
      () => doRetry(job.job_id));
    div.querySelector('[data-action="cancel"]')?.addEventListener('click',
      () => doCancel(job.job_id));
    return div;
  }

  async function doRetry(job_id) {
    await fetch(`/api/jobs/${job_id}/retry`, {method: 'POST'});
    startPolling();
  }

  async function doCancel(job_id) {
    if (!confirm('Zrušit generování?')) return;
    await fetch(`/api/jobs/${job_id}/cancel`, {method: 'POST'});
    startPolling();
  }
```

- [ ] **Step 11.2: Po `doGenerate` zavolat `startPolling()`**

Najít `doGenerate` funkci a po `renderLocations()` volání přidat `startPolling()`:

```javascript
    renderForm(false);
    SELECTED_HIT = null;
    await renderLocations();
    startPolling();   // <-- ADD
  }
```

- [ ] **Step 11.3: Při page load spustit polling, pokud jsou aktivní joby**

Změnit poslední řádek scriptu z `renderLocations();` na:

```javascript
  (async () => {
    await renderLocations();
    const active = await fetch('/api/jobs?active=1').then(r => r.json()).catch(() => []);
    if (active.length > 0) startPolling();
  })();
```

- [ ] **Step 11.4: Manuální smoke test (mock subprocess)**

Pro rychlý test bez čekání 15 min: dočasně v `locations.py` zaměnit `subprocess.Popen` na falešný, který hned tvoří soubor (nebo pustit test slug s rychlým mockem). Alternativně:

```bash
# Vytvořit fake gen_panorama / gen_detail wrappers v PATH, které vyrobí prázdný .glb a vrátí 0
mkdir -p /tmp/fake-bin
cat > /tmp/fake-bin/gen_panorama.py <<EOF
import sys, time
from pathlib import Path
region = sys.argv[sys.argv.index('--region')+1]
Path(f'tiles_v2_{region}').mkdir(parents=True, exist_ok=True)
Path(f'tiles_v2_{region}/panorama.glb').touch()
time.sleep(2)
EOF
# Apod. pro gen_detail.py
```

Ale tohle je over-engineered pro smoke test. Reálný flow:

1. Pomocí UI vygeneruj malou lokaci (např. s `--half 200` ručně, hacknout do `_LOD_PRESET`), ověř že 4-step status se postupně mění
2. Sledovat polling tick každé 2 s
3. Po dokončení Otevřít viewer

Pokud nechceš spotřebovávat ČÚZK budget na test, pošli to do produkce v Task 13 s reálnou lokací (Šantovka).

- [ ] **Step 11.5: Commit**

```bash
git add index.html
git commit -m "feat(index): polling + step status + retry/cancel buttons"
```

---

## Task 12: Diakritika warning + edge polish

**Files:**
- Modify: `index.html`

- [ ] **Step 12.1: Inline warning pod adresním inputem**

V `renderForm` šabloně (`index.html`) přidat pod `<input id="addr-input">`:

```html
<div style="font-size:11px; color:#888; margin-top:4px;">
  Tip: piš s diakritikou (Hnojice, Strážek). RUIAN bez diakritiky nehledá.
</div>
```

- [ ] **Step 12.2: Při 0 hits — fallback na manual S-JTSK input**

V `doSearch`, případ `data.length === 0`, rozšířit:

```javascript
if (data.length === 0) {
  resultsEl.innerHTML = `
    <div class="error">Nic nenalezeno. Zkus i s diakritikou.</div>
    <details style="margin-top:8px;">
      <summary style="cursor:pointer; color:#1a73e8;">Zadat S-JTSK ručně</summary>
      <div style="margin-top:8px;">
        cx: <input id="manual-cx" type="number" step="0.01" style="width:120px">
        cy: <input id="manual-cy" type="number" step="0.01" style="width:120px">
        <button class="btn" id="manual-ok-btn">Použít</button>
      </div>
    </details>
  `;
  setTimeout(() => {
    document.getElementById('manual-ok-btn')?.addEventListener('click', () => {
      const cx = parseFloat(document.getElementById('manual-cx').value);
      const cy = parseFloat(document.getElementById('manual-cy').value);
      if (isNaN(cx) || isNaN(cy)) { alert('Zadej platná čísla'); return; }
      selectHit({label: `Manual ${cx}, ${cy}`, sjtsk_cx: cx, sjtsk_cy: cy, obec: 'lokace'});
    });
  }, 0);
  return;
}
```

- [ ] **Step 12.3: Manuální test**

V prohlížeči:
1. Otevři dashboard → klik [+ Nová lokace]
2. Hledat „xyzqwerty" → 0 hits → vidět „Zadat S-JTSK ručně"
3. Rozbalit, zadat `-547700`, `-1107700`, slug `test-manual`, Generovat → mělo by zařadit

- [ ] **Step 12.4: Commit**

```bash
git add index.html
git commit -m "feat(index): diacritics tip + manual S-JTSK fallback"
```

---

## Task 13: End-to-end smoke test na reálné lokaci (Šantovka)

**Files:**
- (manual test, žádné nové soubory)

- [ ] **Step 13.1: Předpoklady**

- Server běží: `lsof -ti:8080 | xargs -r kill; python3 server.py > /tmp/inzerator-server.log 2>&1 &`
- `cache/dmpok_tiff_*/` může být prázdné — first-run lokace si dlaždice stáhne sama (10–30 min)
- Volný disk: ~2 GB pro novou lokaci (panorama + 3 detaily + ortho cache)

- [ ] **Step 13.2: Vytvořit Šantovku přes UI**

V prohlížeči:
1. Otevři `https://jans-mac-mini.tailfe475e.ts.net/`
2. Klik [+ Nová lokace]
3. Hledej „Šantovka" (s diakritikou!) — pokud RUIAN nemá přesný match na obec „Šantovka", zkus zadat č.p.: „Hodonín 1, Šantovka" nebo manual S-JTSK `-546820.8, -1121852.6` (známé z `gen_multitile.py:702`)
4. Slug auto = `santovka`, label „Šantovka"
5. Klik Generovat

- [ ] **Step 13.3: Sledovat polling**

V UI:
- Vidět nový řádek `Šantovka  ⏳ panorama running…`
- Po ~3-10 min: `Šantovka  ✓ panorama  ⏳ outer running…`
- Apod. pro closeup, inner
- Total time: 10–30 min cold cache, 5–15 min warm

Backend logy (paralelně v shell):
```bash
tail -f /tmp/inzerator-server.log
tail -f cache/jobs/<job_id>/panorama.log
```

- [ ] **Step 13.4: Po dokončení otevřít viewer**

Klik „Otevřít" → `/v2.html?region=santovka` → ověřit:
- Panorama se načte (Šantovkův region, ~30km)
- Detail vrstvy outer/closeup/inner postupně
- Žádné chybové overlay

- [ ] **Step 13.5: Test cancel**

- Klik [+ Nová lokace] → vytvoř `test-cancel` s manual S-JTSK někde, kde gen_panorama bude pomalý
- Po pár sekundách klik Cancel
- Ověřit: job zmizí z polling listu, lokace zůstane jako `partial` v dashboardu (panorama částečně zapsaný), nebo `missing` (nestihla začít zapisovat)
- Cleanup: `rm -rf tiles_v2_test-cancel/`

- [ ] **Step 13.6: Test retry**

- Najít lokaci v `partial` stavu (např. z předchozího cancel testu nebo uměle vytvořenou: `mkdir tiles_v2_test-partial && touch tiles_v2_test-partial/panorama.glb`)
- Restart serveru, refresh dashboard → vidět ⚠ partial + Retry tlačítko
- Klik Retry → ověřit, že worker přesune `panorama` na skipped, ostatní stepy projde
- Pozn.: tahle cesta vyžaduje, aby Retry pro lokaci bez job-id v paměti uměl číst `manifest.json` a vytáhnout `region.center_sjtsk`. **POZOR — to spec popisuje (Resume / retry mechanika, cesta 2), ale dashboard tlačítko Retry tuhle cestu v Tasku 11 NEVOLÁ.** Doplnit:

V `renderLocations` v `index.html`, pro řádek se status `partial`, přidat:

```javascript
${loc.status === 'partial'
  ? `<button class="btn" data-action="retry-from-disk" data-slug="${loc.slug}">Retry</button>`
  : ''}
```

a handler:

```javascript
container.querySelectorAll('[data-action="retry-from-disk"]').forEach(el => {
  el.onclick = async () => {
    const slug = el.dataset.slug;
    // Načíst cx,cy z manifestu (přes /api/locations) nebo z dedikovaného endpointu
    // Stačí poslat slug, backend přečte manifest.
    const r = await fetch('/api/jobs', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({slug, resume_from_disk: true}),
    });
    if (r.status === 200) startPolling();
    else alert('Retry selhal: ' + r.status);
  };
});
```

A v `server.py` v `_handle_post_jobs` podporovat `resume_from_disk` flag:

```python
if body.get("resume_from_disk"):
    manifest_path = Path(f"tiles_v2_{slug}") / "manifest.json"
    if not manifest_path.exists():
        self._send_json(400, {"error": "no manifest.json — partial location must have panorama.glb step done"})
        return
    manifest = json.loads(manifest_path.read_text())
    region = manifest.get("region", {})
    center = region.get("center_sjtsk") or [None, None]
    cx, cy = center[0], center[1]
    label = region.get("label") or slug
    if cx is None or cy is None:
        self._send_json(400, {"error": "manifest missing region.center_sjtsk"})
        return
```

vložit toto nahoře v `_handle_post_jobs` před `cx = body.get("cx")`.

- [ ] **Step 13.7: Update tests a commit nový resume-from-disk handler**

Přidat do `tests/test_locations_api.py`:

```python
def test_resume_from_disk_missing_manifest_400(tmp_path, monkeypatch):
    """Resume bez panorama.glb / manifest.json → 400."""
    status, _ = _post("/api/jobs", {"slug": "nonexistent-partial", "resume_from_disk": True})
    assert status == 400
```

Run: `pytest tests/test_locations_api.py -v`
Expected: PASS.

- [ ] **Step 13.8: Final commit**

```bash
git add index.html server.py tests/test_locations_api.py
git commit -m "feat: resume-from-disk retry for partial lokace bez in-memory job"
```

- [ ] **Step 13.9: Final acceptance**

V UI:
- Dashboard má alespoň 2 lokace (Hnojice + nová)
- Nová lokace jde otevřít přes viewer a renderuje se správně
- Retry funguje pro `partial` (i po restartu serveru)
- Cancel zabije běžící proces do 10 s
- Diakritika tip se zobrazuje
- Manual S-JTSK fallback funguje

---

## Self-Review (provedeno autorem plánu)

**1. Spec coverage:**
- ✅ Brainstorming volby B/B/A/B/C/B/B — promítnuto do tasků (RUIAN search Task 4, dashboard Task 9, fixní LOD Task 7, polling Task 11, slug auto-fill Task 10, FIFO queue Task 5, resume Task 7)
- ✅ ČÚZK RUIAN AdresniMisto endpoint (Task 4) odpovídá spec sekci „RUIAN search"
- ✅ Inner step 0.5 m (Task 7 cmd_for, test `test_cmd_for_inner_has_step_0_5`)
- ✅ Cancel endpoint (Task 6 state, Task 7 subprocess.terminate, Task 8 HTTP handler, Task 11 UI button)
- ✅ Cold cache 60 min timeout (Task 7 STEP_TIMEOUT_SECS)
- ✅ stderr head 300 + tail 300 (Task 7 `_format_error`)
- ✅ Pytest threading testy (Task 5–7)
- ✅ Resume-from-disk po restartu (Task 13.6–7)
- ✅ index.html jako root „/" landing (Task 9)
- ✅ Diakritika UI warning (Task 12)

**2. Placeholder scan:** žádné „TBD/TODO/fill in details". Všechny kódy v cele.

**3. Type consistency:**
- `JOBS[id] = {slug, label, cx, cy, steps[], cancelled, created_at, job_id}` — používáno konzistentně napříč Task 5/6/7/8.
- `step = {name, state, error, started_at, finished_at}` — konzistentní.
- `cmd_for(step_name, slug, cx, cy)` — Task 7 signature je stejná v testech i implementaci.
- `expected_glb(slug, step_name)` — taktéž.

**4. Drobné rezervy:**
- `JOB_LOG_DIR = Path("cache/jobs")` se vyhodnocuje relativně k cwd. Worker thread běží ve stejném cwd jako server (kde se pouští `python3 server.py`). Pokud bys spustil server z jiného adresáře, logy půjdou jinam — předpokládá se konvence „spouštět z repo root". Stejně to dělají `gen_panorama.py` a `gen_detail.py`.
- Test `test_ruian_search_escapes_quotes_and_percent` má dvě alternativní assertions (`O%27%27Hara` nebo `O''Hara`); urlencode standardně vrací `%27`, takže first branch projde.
