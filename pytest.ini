[pytest]
; Bare `pytest` used to collect from the repo root and walk the cache/ and
; cuzk-pyramid/ symlinks (the latter is an 888 GB tile tree on an external
; drive) — collection alone burned minutes of CPU before running a test.
testpaths = tests
